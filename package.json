{
  "name": "cypress-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run": "npx cypress run",
    "merge:reports": "mochawesome-merge cypress/reports/mochawesome/*.json > cypress/reports/mochawesome/merged.json",
    "generate:report": "marge cypress/reports/mochawesome/merged.json -o cypress/reports/mochawesome",
    "cypress:test": "npm run cypress:run && npm run merge:reports && npm run generate:report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^6.6.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.6.1",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
